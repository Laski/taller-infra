# Version control
.git/
.gitignore

# Docs and build config
AGENTS.md
README.md
Dockerfile
.dockerignore

# Dependencies and build output (rebuilt inside the container)
node_modules/
dist/

# Local environment files (never in the image)
.env
.env.*
*.local
